User Flow:
user will come to app add pariticpant
user willl lgo expense
user will select splitting strategy.
net balances of user. -> Balance sheet.
user will get Transaction.

splittign strategy -> ?? 
min. no of transactions.


Core Entities -> User, Expense, Transaction, BalanceSheet

Services -> ExpenseService. 

split -> strategy design pattern
Balance sheet needs to get notified in each expense when it will get added so we are gonna use a observer pattern over thtere.

lets start coign

